<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.appabilities.complainmanagementsystem">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".screens.splash.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screens.registration_module.RegistrationModuleActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".screens.bottom_nav.BottomNavActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".screens.complain_details.ComplainDetailsActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".screens.add_complains.AddComplainActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

    </application>

</manifest>